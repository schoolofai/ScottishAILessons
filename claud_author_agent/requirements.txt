# Claude Agent SDK - Core dependency
claude-agent-sdk>=0.1.3

# Async runtime
anyio>=4.0.0

# Appwrite SDK - For direct database access in validation
appwrite>=5.0.0

# Optional: Testing dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Optional: Type checking
mypy>=1.5.0

# Optional: PDF export for revision notes
# Note: weasyprint has system dependencies (Cairo, Pango)
# See: https://doc.courtbouillon.org/weasyprint/stable/first_steps.html#installation
weasyprint>=60.0
